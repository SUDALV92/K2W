/// scrRedSetPolarCoordinates( centerX, centerY, radius, direction )
var startX = argument0;
var startY = argument1;
var radius = argument2;
var dir = argument3;
x = startX + lengthdir_x( radius, dir );
y = startY + lengthdir_y( radius, dir );
